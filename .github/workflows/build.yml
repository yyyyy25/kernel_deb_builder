name: Build kernel deb packages
on:
  workflow_dispatch:
  push:
  schedule:
    - cron: "0 0 * * *"

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        version: ["5.19.0"]
    name: Build kernel
    runs-on: ubuntu-22.04
    steps:
      - name: Maximize build space
        uses: easimon/maximize-build-space@master
        with:
          root-reserve-mb: 4096
          swap-size-mb: 512
          remove-dotnet: 'true'
          remove-android: 'true'

      - name: start docker and build 
        run: |
          docker run --rm --env VERSION=${{ matrix.version }} \
            -v ${{ github.workspace }}/linux-${{ matrix.version }}:/linux-${{ matrix.version }} \
            -v ${{ github.workspace }}/config-${{ matrix.version }}:/config-${{ matrix.version }} \
            -v ${{ github.workspace }}/entrypoint.sh:/entrypoint.sh \
            -v ${{ github.workspace }}/patch.d/:/patch.d/ \
            gcc:bullseye bash /entrypoint.sh

      - name: Checkout
        uses: actions/checkout@v2

      - name: Build
        run: bash build_action.sh

      - name: Artifact
        uses: actions/upload-artifact@v2
        with:
          name: linux-${{ matrix.version }}
          path: ${{ github.workspace }}/linux-${{ matrix.version }}/